import React from "react";
import MainCarousel from "./MainCarousel";

type Props = {};

const Home = (props: Props) => {
  return (
    <div className="home">
      <MainCarousel />
    </div>
  );
};

export default Home;
